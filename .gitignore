# Ignore all mp3 files in audio_input directory
audio_input/*.mp3

# Keep the directories but ignore their contents (specifically the 'empty' files)
audio_input/empty
persistent_data/empty
transcriptions_output/empty